<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.media.MediaView?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1" fx:controller="hellofx.MainController" stylesheets="@style.css">

    <!-- Barra de menú superior -->
    <top>
        <MenuBar fx:id="menuBar">
            <Menu text="Archivo">
                <MenuItem text="Abrir" onAction="#onAbrirMenuItemClicked"/>
                <MenuItem text="Salir" onAction="#onSalirMenuItemClicked"/>
            </Menu>
            <Menu text="Biblioteca">
                <MenuItem text="Actualizar"/>
            </Menu>
            <Menu text="Ver">
                <MenuItem text="Pantalla Completa" onAction="#onFullscreenButtonClicked"/>
            </Menu>
            <Menu text="Acerca">
                <MenuItem text="Información"/>
            </Menu>
        </MenuBar>
    </top>

    <!-- Área central -->
    <center>
        <StackPane fx:id="mediaContainer" style="-fx-background-color: black;">
            <MediaView fx:id="mediaView" preserveRatio="true"/>
        </StackPane>
    </center>

    <!-- Barra de controles inferior -->
    <bottom>
        <VBox fx:id ="container" spacing="5" style="-fx-padding: 10px; -fx-background-color: #000;">
            <Slider fx:id="progressSlider" min="0" max="100" value="0" style="-fx-pref-width: 500px;"/>
            <HBox spacing="10" alignment="CENTER">
                <Button text="▶" fx:id="playButton" onAction="#onPlayButtonClicked"/>
                <Button text="⏸" fx:id="pauseButton" onAction="#onPauseButtonClicked"/>
                <Button text="⏹" fx:id="stopButton" onAction="#onStopButtonClicked"/>
                <Label text="🔊"/>
                <Slider fx:id="volumeSlider" min="0" max="1" value="0.5" style="-fx-pref-width: 150px;"/>
                <Button text="⛶" fx:id="fullscreenButton" onAction="#onFullscreenButtonClicked"/>
                <Button text="x2" fx:id="speedButton" onAction="#onSpeedButtonClicked" style="-fx-pref-width: 50px;"/>
            </HBox>
        </VBox>
    </bottom>

    <!-- Panel lateral izquierdo -->
    <left>
        <VBox fx:id="leftPanel" spacing="10" style="-fx-padding: 10px; -fx-background-color:rgb(0, 0, 0);">
            <Button text="▲" onAction="#onLeftPanelToggleClicked" style="-fx-pref-width: 50px;"/>
            <Label text="Editor de Video" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
            <Button text="Cambiar Tamaño" onAction="#onResizeButtonClicked"/>
            <Button text="Cambiar Velocidad" onAction="#onSpeedButtonClicked"/>
        </VBox>
    </left>

    <!-- Panel lateral derecho -->
    <right>
        <VBox fx:id="rightPanel" spacing="10" style="-fx-padding: 10px; -fx-background-color:rgb(0, 0, 0);">
            <Button text="▲" onAction="#onRightPanelToggleClicked" style="-fx-pref-width: 50px;"/>
            <Label text="Biblioteca" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
            <ListView fx:id="fileListView"/>
        </VBox>
    </right>
</BorderPane>
